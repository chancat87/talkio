## What's New in v2.2.2

### ✨ New Features
- 群聊新增「同时回复」模式，所有模型并发生成回复
- 群聊设置面板点击聊天区域自动收起（桌面端 + 移动端）

### 🐛 Bug Fixes
- 修复 MCP SSE 响应使用 ReadableStream 导致 Tauri 连接 hang 住
- 群聊转发消息时自动去除 `<think>` / `<thinking>` 标签，防止其他模型看到推理内容
- 修复停止按钮和发送按钮重叠问题，停止按钮移入输入框内替代发送按钮
- 修复麦克风按钮与发送按钮未右对齐

### 💄 Improvements
- 模型名与时间文字改用基线对齐
- 群聊编辑标题图标移到标题左侧，与标题同行显示
